# Browser-only app — no server, no secrets. The only optional key is Census
# (free, public-data, safe to expose client-side): https://api.census.gov/data/key_signup.html
# Works without a key at low volume; a key avoids throttling during judging.
VITE_CENSUS_API_KEY=
